# Webclinic (monorepo)

This repo contains a Next.js frontend (`frontend/`) and a Go Fiber backend (`backend/`).

See `frontend/README.md` and `backend/README.md` for component-specific instructions.
